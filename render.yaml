services:
  - type: web
    name: treasury
    env: node
    plan: free
    buildCommand: yarn install && yarn prisma generate && yarn build
    startCommand: yarn prisma db push && yarn prisma db seed && node build/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: AUTH_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromService:
          type: web
          name: treasury
          envVarKey: DATABASE_URL
      - key: HOST
        fromService:
          type: web
          name: treasury
          envVarKey: HOST
      - key: ORIGIN
        fromService:
          type: web
          name: treasury
          envVarKey: ORIGIN
      - key: PORT
        fromService:
          type: web
          name: treasury
          envVarKey: PORT
      - key: GROQ_API_KEY
        fromService:
          type: web
          name: treasury
          envVarKey: GROQ_API_KEY
      - key: GEMINI_API_KEY
        fromService:
          type: web
          name: treasury
          envVarKey: GEMINI_API_KEY
      - key: GROQ_MODELS
        fromService:
          type: web
          name: treasury
          envVarKey: GROQ_MODELS
      - key: GEMINI_MODELS
        fromService:
          type: web
          name: treasury
          envVarKey: GEMINI_MODELS
      - key: DEFAULT_GROQ_MODEL
        fromService:
          type: web
          name: treasury
          envVarKey: DEFAULT_GROQ_MODEL
      - key: DEFAULT_GEMINI_MODEL
        fromService:
          type: web
          name: treasury
          envVarKey: DEFAULT_GEMINI_MODEL
      - key: GOOGLE_CLIENT_ID
        fromService:
          type: web
          name: treasury
          envVarKey: GOOGLE_CLIENT_ID
      - key: GOOGLE_CLIENT_SECRET
        fromService:
          type: web
          name: treasury
          envVarKey: GOOGLE_CLIENT_SECRET